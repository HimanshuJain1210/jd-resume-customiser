// The pdfjs worker module is imported for its side effect (registering the
// worker so Next bundles pdf.worker.mjs into the function). It ships no types.
declare module "pdfjs-dist/legacy/build/pdf.worker.mjs";
